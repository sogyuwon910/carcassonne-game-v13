# 전체 설계

## 핵심 선택

- 보드는 `Map<"x,y", PlacedTile>`로 저장합니다. 고정 크기 배열이 없어 어느 방향으로든 확장할 수 있습니다.
- 화면은 Canvas로 그립니다. 수십 장의 PNG를 회전하면서 드래그·확대·축소하기에 DOM 타일보다 좌표 변환이 단순합니다.
- 규칙 엔진은 Canvas와 DOM을 모릅니다. 따라서 자동 테스트와 AI가 사람 플레이와 정확히 같은 배치·연결·점수 규칙을 재사용합니다.
- 성과 도로는 **타일 내부 영역**을 노드로 보고, 맞닿은 같은 지형 영역을 연결해 탐색합니다. 열린 가장자리가 하나도 없으면 완성입니다.

## 연결 판정 방식

타일 데이터의 `city_groups`, `road_groups`가 한 타일 안에서 연결된 방향 묶음을 표현합니다. `featureGraph.js`는 선택한 영역에서 시작해 다음을 반복합니다.

1. 현재 영역의 방향을 확인합니다.
2. 해당 방향에 타일이 없으면 열린 끝으로 기록합니다.
3. 타일이 있으면 반대 방향에서 맞닿는 같은 종류의 내부 영역을 찾습니다.
4. 아직 방문하지 않은 영역을 계속 탐색합니다.

이 방식은 여러 타일을 잇는 구조물, 서로 분리된 두 영역, 나중에 하나로 합쳐지는 구조물, 고리 형태를 같은 코드로 처리합니다. 마을로 들어가는 도로는 JSON에서 서로 다른 `road_groups`로 분리되어 있으므로 자동으로 독립 도로가 됩니다.

장점은 규칙과 데이터의 관계가 명확하고 테스트하기 쉽다는 점입니다. 단점은 AI 후보마다 여러 번 탐색하면 계산량이 늘어난다는 점입니다. 현재 덱 규모에서는 충분히 동작하며, 규칙을 단순화하는 성능 최적화는 적용하지 않았습니다.

## 게임 상태 흐름

`DRAW_TILE → ROTATE_OR_PLACE → PLACE_MEEPLE_OR_SKIP → SCORE_COMPLETED_FEATURES → NEXT_TURN` 순서를 `game.js`가 관리합니다. 덱이 끝나면 `GAME_OVER`로 전환하고 미완성 구조물을 한 번씩 계산합니다.

## 기본 게임 10단계 대응

1. 시작 화면과 플레이어 설정 — `index.html`, `ui.js`
2. 타일 데이터와 회전 — `tile.js`, `tiles.json`
3. 좌표 보드와 배치 규칙 — `board.js`
4. 카메라·PNG·확대축소 — `renderer.js`
5. 도로 연결과 완성 — `featureGraph.js`
6. 성 연결과 완성 — `featureGraph.js`
7. 수도원과 미플 — `featureGraph.js`, `game.js`
8. 점수와 턴 — `scoring.js`, `game.js`
9. 종료 점수와 순위 — `scoring.js`, `ui.js`
10. 그래픽·반응형·전체 테스트 — `styles.css`, `tests/`

## AI 구조

`ai.js`는 가능한 모든 합법 행동을 만들고, 각 행동을 `GameEngine.simulateAction()`으로 복사 상태에 임시 적용합니다. 시뮬레이션은 배치·미플·완성 점수·미플 회수까지 기존 게임 흐름을 그대로 실행합니다. 실제 상태와 규칙 코드는 수정하지 않습니다.

`aiValues.js`가 `C_p(S)`, `N_p(S)`, `I_p(S)=C_p(S)+N_p(S)`, 기존 미래 가치 `F_p(S)`, 남은 자기 턴 수와 후보 결과의 `m_i`/`r_i`를 계산합니다. `r_i`는 행동 전후 자기 미플 수와 해당 행동의 미플 배치 여부로 계산하므로 상대 회수분은 들어가지 않습니다.

공통 실행 순서는 `합법 후보 생성 → prepareTurnContext → filterActions → 복제 상태 evaluateAction → selectAction → describeStrategy`입니다. 턴마다 한 번만 정해야 하는 난수와 상대 기준 플레이어는 `prepareTurnContext`에 저장되어 후보 평가에서 다시 뽑히지 않습니다. 일반 최대값 동점과 전략이 명시한 무작위 후보 선택만 공통 난수 선택기를 사용합니다.

## AI 프로필과 전략 레지스트리

`aiProfiles.js`의 프로필은 `id`, `name`, `description`, `strategyId`만 관리합니다. `aiStrategies.js`의 `AI_STRATEGIES`는 8개 프로필마다 별도 전략 객체를 등록합니다. 기본/허서연/소규원도 현재 공식은 같지만 객체와 전략 ID가 다르므로 독립 수정할 수 있습니다.

- 기본·허서연·소규원: `V_i=I_i+F_i`
- 이수완: 턴별 `k`와 현재 미플에 따라 `I/F` 가중치를 바꾸고, `m<=3`이면 한 번의 확률 판정으로 미플 후보를 제한
- 정재이: 현재 기본 가치가 가장 큰 플레이어 `A`를 한 번 고르고 상대라면 후보 가치에서 `F_A(S_i)`만 차감
- 장성이: 가능한 수도원 미플을 우선하고 `V_i=2C_i+N_i+F_i+5/(m_i+1)` 사용
- 손건후: 남은 턴/낮은 미플과 `N` 조건으로 미플을 강제하고 자기 회수 `r_i`를 보상
- 김규민: 상대 중 현재 `C` 선두를 골라 조건부로 `F_A(S_i)=0` 후보를 최우선 선택하고 남은 턴 미플 조건을 적용

조언은 `chooseAiAdvice`가 `cloneForSimulation()`으로 만든 복사본을 다시 후보별로 복사해 계산합니다. 배치 단계는 타일+미플 결합 행동을, 미플 단계는 이미 놓은 타일의 미플/건너뛰기 행동만 생성합니다. 조언 기본 난수는 원본 `GameEngine.random`을 소비하지 않으며 테스트에서는 별도 난수를 주입할 수 있습니다.

`ui.js`의 `renderAiStrategyReport`는 AI 로그와 조언에 같은 구조의 설명을 렌더링합니다. 출력용 디버그 데이터도 깊게 복사해 전략의 내부 턴 컨텍스트와 참조를 공유하지 않습니다.

## 읽기 전용 행동 오버레이

`actionPreview.js`는 AI 행동을 좌표·회전·미플 구조물 정보가 있는 표시 데이터로 바꿉니다. `renderer.js`는 이 데이터를 별도 `actionPreview` 필드에만 보관합니다. 보드·덱·점수에는 쓰지 않으며 Canvas의 마지막 그리기 계층에서 반투명 타일과 강조 테두리, 미플 마커를 그립니다.

## 비파괴 기록 트리

`GameEngine.serializeState()`는 보드와 덱의 `TileInstance`를 타일 정의 ID와 회전 값으로 바꾸고 `Map`/`Set`도 배열로 바꿉니다. `HistoryTree`는 이 순수 데이터를 다시 깊게 복사해 부모/자식 노드에 저장합니다. `restoreState()`는 타일 정의를 재연결하되 보드·미플·플레이어 객체를 모두 새로 만듭니다.

현재 노드가 실제 진행 끝(`liveNodeId`)과 다를 때 행동을 시작하면 `pendingBranch`가 켜집니다. 턴 완료 후 새 자식 노드를 만들면서 새 분기 ID를 부여하고, 기존 자식 노드는 그대로 남깁니다. 다음 이동의 기본값은 `original` 자식이며 UI의 분기 선택으로 다른 자식도 열 수 있습니다.
